= simple_form_for @venue, :url => { :action => "create" }, :remote => true, :html => {:method => :create, :class => 'form-horizontal' } do |f|
  = f.input :address, :size => "60x12" 
  = f.input :zipcode
  = f.input :city 
  = f.input :state
  = f.submit "Create", :class => "btn btn-primary"
